import 'package:flutter/material.dart';

class ForCodeWidget extends StatelessWidget {
  const ForCodeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [],
    );
  }
}
